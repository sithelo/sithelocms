#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-docker/node_modules/grunt/node_modules/grunt-legacy-util/node_modules/which/bin/which" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-docker/node_modules/grunt/node_modules/grunt-legacy-util/node_modules/which/bin/which" "$@"
  ret=$?
fi
exit $ret
